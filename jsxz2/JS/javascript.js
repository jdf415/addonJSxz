// newH1 = document.createElement("h1");

// newH1.textContent = "hi =)";

// document.body.appendChild(newH1);


// returns data type of an object - evaluates to an object
console.log(document.getElementById('div-two'));

// returns an array with objects calls called HTML collection - evaluates to HTML collection
console.log(document.getElementsByClassName('box'));

// adding a specific index will return an element object
console.log(document.getElementsByClassName('box')[1]);


// returns html collection of all elements with specific tag name as an HTML collection
console.log(document.getElementsByTagName('div'));

